#include <stdio.h>
#include <stdlib.h>

struct	s_node {
		int				value;
		struct s_node	**nodes;
};

int		height_tree(struct s_node *root)
{
	struct	s_node		holder = NULL;
	
	if (!(root))
		return (-1);
	while (holder != NULL)
	{
		if (root->)
	}
}

int		main(void)
{
}
